import {
  Button as GButton,
  Input as GInput,
  Switch as GSwitch,
  Tag as GTag,
  Checkbox as GCheckbox,
  Pagination as GPagination,
  Spin as GSpin,
  Modal as GModal,
} from 'ant-design-vue';

export { default as GStickyTable } from './GStickyTable.vue';

export { GButton, GInput, GSwitch, GTag, GCheckbox, GPagination, GSpin, GModal };
